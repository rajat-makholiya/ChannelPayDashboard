import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    info: {
      main: "#6B6B6B",
    },
    primary: {
      main: "#212121",
    },
    secondary: {
      main: "#D0CFCF",
    },
  },
});

export default theme;
